{
  "name": "@just-every/ultracode",
  "description": "Ultracode Codex plugin implementation.",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/just-every/plugin-ultracode.git"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "pretest": "npm run build",
    "test": "node --test \"test/**/*.test.js\"",
    "pretypecheck": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.core.json"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  }
}
